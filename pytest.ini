[pytest]
testpaths = tests
python_files = test_*.py
# Backend package imports rely on the repository root; conftest.py adds it.
# Keep asyncio/httpx TestClient warnings loud rather than silenced.
filterwarnings =
    error::DeprecationWarning:backend.*
