# SettleSense — Python dependencies
# Domain engine (increment 1)
pandas>=2.2
pydantic>=2.6

# Service layer (phase 1)
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27           # API tests via fastapi.testclient
python-multipart>=0.0.9 # multipart uploads for POST /api/v1/batches

# Testing
pytest>=8.0
