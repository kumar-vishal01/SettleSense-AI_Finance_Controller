import { useState } from "react";
import { Icon } from "../components/icons";

export default function Auth({ mode, onBack, onContinue }: { mode: "login" | "signup"; onBack: () => void; onContinue: () => void }) {
  const signup = mode === "signup";
  const [showPassword, setShowPassword] = useState(false);
  return <div className="flex min-h-screen bg-[#f8fafc] text-ink"><div className="hidden w-[42%] flex-col justify-between bg-[#07152f] p-10 text-white lg:flex"><button onClick={onBack} className="flex items-center gap-2 text-left text-lg font-bold"><span className="flex h-9 w-9 items-center justify-center rounded-xl bg-[#0042e2]"><Icon name="layers" className="h-5 w-5" /></span>SettleSense</button><div><p className="max-w-md text-4xl font-bold leading-tight">A clearer close starts with the evidence.</p><p className="mt-5 max-w-sm text-sm leading-6 text-white/55">Reconcile the money movement your team actually needs to trust.</p></div><p className="text-xs text-white/40">AI explains. The deterministic engine decides.</p></div><main className="flex flex-1 items-center justify-center px-5 py-10"><div className="w-full max-w-md animate-fade-up"><button onClick={onBack} className="mb-10 flex items-center gap-2 text-sm font-semibold text-ink-2 hover:text-ink"><Icon name="chevron" className="h-4 w-4 rotate-180" /> Back to home</button><p className="text-xs font-bold uppercase tracking-[.15em] text-[#0042e2]">{signup ? "Create workspace" : "Welcome back"}</p><h1 className="mt-3 text-3xl font-bold tracking-tight">{signup ? "Start with a demo batch" : "Sign in to SettleSense"}</h1><p className="mt-2 text-sm text-ink-2">{signup ? "No connection required for the seeded demo." : "Continue to your finance control room."}</p><form onSubmit={e => { e.preventDefault(); onContinue(); }} className="mt-8 space-y-4">{signup && <label className="block text-sm font-medium">Full name<input required className="mt-1.5 w-full rounded-lg border border-[#d5deeb] bg-white px-3 py-2.5 outline-none transition focus:border-[#0042e2] focus:ring-4 focus:ring-[#0042e2]/10" placeholder="Ava Morgan" /></label>}<label className="block text-sm font-medium">Work email<input required type="email" className="mt-1.5 w-full rounded-lg border border-[#d5deeb] bg-white px-3 py-2.5 outline-none transition focus:border-[#0042e2] focus:ring-4 focus:ring-[#0042e2]/10" placeholder="you@company.com" /></label><label className="block text-sm font-medium">Password<div className="relative mt-1.5"><input required minLength={8} type={showPassword ? "text" : "password"} className="w-full rounded-lg border border-[#d5deeb] bg-white px-3 py-2.5 pr-10 outline-none transition focus:border-[#0042e2] focus:ring-4 focus:ring-[#0042e2]/10" placeholder="8+ characters" /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-2.5 text-ink-2" aria-label={showPassword ? "Hide password" : "Show password"}><Icon name="eye" className="h-4 w-4" /></button></div></label>{signup && <label className="flex items-start gap-2 text-xs text-ink-2"><input required type="checkbox" className="mt-0.5 accent-[#0042e2]" /> I agree to the terms and privacy policy.</label>}<button className="btn btn-primary w-full py-3">{signup ? "Create demo workspace" : "Continue to workspace"}<Icon name="arrowright" className="h-4 w-4" /></button></form><p className="mt-6 text-center text-sm text-ink-2">{signup ? "Already have an account?" : "Don't have an account?"} <button onClick={() => onContinue()} className="font-semibold text-[#0042e2]">{signup ? "Sign in" : "Create one"}</button></p></div></main></div>;
}